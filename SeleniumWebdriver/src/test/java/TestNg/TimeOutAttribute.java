package TestNg;

public class TimeOutAttribute {
	
	/*
	 If we want to run perticular testcase within the time the we have to use 
	 timeOut Attribute in testNG
	 
	 if testcase is not run within the perticular time the the test case will
	 fail
	 
	 syntax - 
	 
	 @Test(timeOut=8000) 8000 is millisesond
	 */

}
